version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: v16.16.0..x
  build:
    commands:
      - sam build
  # post_build:
  #   commands:
  #     - echo Build completed on `date`
artifacts:
  type: zip
  files:
    - template.yaml